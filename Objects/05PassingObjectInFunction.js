const user = {
    username: "imankit2705",
    mail: "ankit@gmail.com"
}

function handleObject(anyObject) {
    console.log(`Username is "${anyObject.username}" and mail is "${anyObject.mail}"`)
}

handleObject(user);